INSERT INTO vw_regions_exits (
  cartodb_id,
  the_geom,
  the_geom_webmercator,
  user_journey_ID,
  user_id,
  local_month,
  travel_month,
  EntryType,
  gz_id,
  entry_time,
  in_region_datetime1,
  in_region_datetime2,
  exit_time,
  entered_from_region,
  tourism_region_name,
  went_to_region,
  latitude,
  longitude,
  state,
  crossing_in_duration,
  crossing_out_duration,
  time_in_region_hours,
  active,
  country,
  Source)
SELECT
(cartodb_id + {maxid}) AS cartodb_id,
the_geom,
the_geom_webmercator,
user_journey_ID,
user_id,
local_month,
travel_month,
EntryType,
gz_id,
entry_time,
in_region_datetime1,
in_region_datetime2,
exit_time,
entered_from_region,
tourism_region_name,
went_to_region,
latitude,
longitude,
state,
crossing_in_duration,
crossing_out_duration,
time_in_region_hours,
active,
country,
Source
FROM {source}
WHERE cartodb_id <= 100000
